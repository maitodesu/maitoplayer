{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "main-capability",
  "description": "Minimum application window capability; filesystem and shell access are intentionally absent.",
  "windows": ["main"],
  "permissions": ["core:default"]
}

