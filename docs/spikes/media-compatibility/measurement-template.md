# Reference measurement

- Date / operator:
- Windows build / WebView2 version:
- CPU / GPU / driver:
- FFmpeg / FFprobe version:
- Fixture hash:
- Playback path: direct | remux | convert-audio | transcode-video
- Time to first frame (ms):
- Seek-to-frame p50 / p95 (ms):
- Dropped frames / 10 minutes:
- Working set after warm-up / after 2 hours (MiB):
- GPU engine utilization:
- Range request maximum bytes:
- Canvas allocation released:
- Cancellation child/partial cleanup:
- Result / notes:

